let name = window.prompt(`Qual seu nome?`)
document.write(`Seu nome em letra maiscula: ${name.toUpperCase()}`)
document.write(`Seu nome em letra minuscula: ${name.toLowerCase()}`)
document.write(`Seu nome tem ${name.length} letras`)
